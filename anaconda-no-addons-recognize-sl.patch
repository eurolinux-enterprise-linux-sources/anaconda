--- yuminstall.py	2012-06-12 14:40:09.000000000 -0500
+++ yuminstall.py	2012-06-28 14:21:51.405887644 -0500
@@ -600,6 +600,12 @@ class AnacondaYum(YumSorter):
             del(repo)
             raise RepoError, "Excluding devel repo %s for non-devel anaconda" % name
 
+        # CJS this is very hard-coded, we need to not have the installer see these
+        if ("sl" == repo.id or "sl-security" == repo.id):
+            name = repo.name
+            del(repo)
+            raise RepoError, "Excluding repo %s for anaconda" % name
+
         if not productIsFinal and not repo.enabled:
             name = repo.name
             del(repo)
@@ -883,7 +889,10 @@ class AnacondaYum(YumSorter):
 
         initialRepos = self.repos.repos.values() + extraRepos
         for repo in initialRepos:
-            addons = self._getAddons(repo)
+#CJS addons are only for TUV , this code causes added repos turn networking
+# on for DVD/media installs.  We are disabling it.
+#           addons = self._getAddons(repo)
+            addons = []
             for addon in addons:
                 addonRepo = AnacondaYumRepo(addon[0])
                 addonRepo.name = addon[1]
